# Secrets go here. Change it, and rename it .env

# If you want something but PostgreSQL and asynpg, you can try. Success is not guaranteed.
# format is: postgresql+asyncpg://user:password@host/database
BLOG_DEVELOPMENT_DB_CONNECTION_STRING="postgresql+asyncpg://user:password@host/database"
BLOG_PRODUCTION_DB_CONNECTION_STRING="postgresql+asyncpg://user:password@host/database"

# Credentials for send mail.
MAIL_USERNAME="me@gmail.com"
MAIL_PASSWORD="password"
